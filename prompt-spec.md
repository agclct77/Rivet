# 工單 002-M1：WPF 殼建立與功能移植（手動啟動版）

## 背景
- 目前僅有 `Rivet.Console`，熱鍵與常駐功能將於後續里程碑實作。
- 依 `reference/002-hotkeys/milestones.md` 的 M1，先建立 WPF 殼並確保既有翻譯功能可在 WPF 正常運作。

## 目標
- 新增 `Rivet.WPF` 專案，手動啟動即可完成「讀剪貼簿 → 翻譯（中↔英）→ 顯示結果」，暫時仍可回寫剪貼簿以確保可用性。
- 保留既有 Service / Infrastructure / DI 設計，避免重寫業務與外部服務邏輯。
- 提供可建置/發行的 WPF 版本（無須熱鍵、常駐、托盤、單實例）。

## 範圍
### In Scope
- 新專案：`src/Rivet.WPF/`（.csproj、App.xaml、MainWindow）。
- DI 接線：重用 `Rivet.Service`、`Rivet.Infrastructure` 的組態與註冊邏輯（可抽取/複用原 Console 的組態方法）。
- 功能流程：
	1) 手動啟動 WPF App。
	2) 讀取剪貼簿內容。
	3) 呼叫既有翻譯服務（中↔英）。
	4) 顯示譯文於 UI；暫時可回寫剪貼簿（與 Console 行為一致）。
- 錯誤處理與日誌：沿用既有 Serilog/設定方式，錯誤需有使用者提示。
- 發行/建置：可 `dotnet build` / `dotnet publish` 產出 WPF 執行檔。

### Out of Scope（M1 不做）
- 全域熱鍵 / RegisterHotKey / 訊息迴圈處理。
- 常駐 / 系統匣 / 單實例 / NotifyIcon。
- 並行譯文視窗、剪貼簿回寫策略調整（在後續里程碑處理）。
- UI/UX 美化（以最小可用 UI 為主）。

## 功能需求
1) 啟動與 UI
- 提供 MainWindow（或等效視圖）可手動觸發翻譯（按鈕/命令皆可）。
- 翻譯結果需顯示在 UI（例如文字框或結果區域），同時可保持回寫剪貼簿以利快速使用。

2) 翻譯流程
- 使用現有剪貼簿服務與翻譯服務；若既有方向選擇需沿用（中→英、英→中）。
- 讀取剪貼簿為空或非文字時，需提示錯誤，不應崩潰。

3) 日誌與錯誤
- 沿用 Serilog 設定；錯誤情境（剪貼簿空、翻譯失敗）需有 UI 提示。

4) 建置/發行
- 能以 `dotnet build`、`dotnet publish` 針對 WPF 專案產出可執行檔（Debug/Release）。

## 非功能需求
- 相容性：Windows 10/11，.NET 8，同層級權限執行（無需管理員）。
- 效能：無需熱鍵迴圈；啟動與翻譯延遲與 Console 版相當。
- 可維護性：DI 與服務註冊可複用；避免重複業務邏輯。

## 交付與驗收
- 手動啟動 WPF App 可完成剪貼簿翻譯（中↔英）、結果顯示，並回寫剪貼簿。
- 剪貼簿為空或非文字時，顯示友善錯誤提示，程式不崩潰。
- `dotnet publish` 能輸出可執行檔；日誌落地與 Console 版一致（路徑/格式）。

## 備註
- 後續里程碑（M2+）將調整 UI/UX（不回寫剪貼簿）、加入熱鍵/常駐/單實例等功能，此工單不涵蓋。

